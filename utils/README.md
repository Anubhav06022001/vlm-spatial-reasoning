# Utilities Directory
This folder provides supportive global functions to ensure script reproducibility and clean console logs.

* **logger.py**: Configures explicit runtime data printing formats and saves console traces to disk automatically.
* **seed.py**: Freezes global environment randomization factors across NumPy, PyTorch, and random packages to guarantee consistent outputs.