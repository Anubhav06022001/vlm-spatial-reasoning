# Datasets Directory
This folder manages the data downloading and preprocessing pipelines for spatial fine-tuning.

* **preprocess.py**: Handles dataset fetching, spatial task filtering, dynamic multiple-choice generation, and text tokenization to format raw LLaVA-Instruct data for VLM input.